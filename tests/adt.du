
fun adt_fun (x : ((I32, I32) | I32), cont : fun (_:I32)) = ifcase 0 x then else;
fun then (y : (I32, I32)) = cont 12i32;
fun else () = ifcase 1 x then2 else2;
fun then2 (z : I32) = cont z;
# TODO add `unreachable`
fun else2 () = cont 184i32;
